// eslint-disable-next-line no-useless-escape
const emailRegexp = /^\w+([\.-]?\w+)*@\w+([\.-]?\w+)*(\.\w{2,3})+$/;
const starterSubscr = "starter";
const proSubscr = "pro";
const businessSubscr = "business";

module.exports = {
  emailRegexp,
  starterSubscr,
  proSubscr,
  businessSubscr,
};
